*.js
!jest.config.js
*.d.ts
node_modules

# Lambda .NET — build MSBuild / NuGet (ne pas utiliser **/bin/ : le dossier CDK bin/ doit rester versionné)
src/bin/
src/obj/
src/publish
src/lambda.zip

# Visual Studio / VS Code / JetBrains
.vs/
.idea/
*.user
*.suo
*.userosscache

# Tests & couverture
TestResults/
[Tt]est[Rr]esult*/
*.trx
coverage*.json
coverage*.xml

# AWS SAM (optionnel)
.aws-sam/

# CDK asset staging directory
.cdk.staging
cdk.out
output.zip
